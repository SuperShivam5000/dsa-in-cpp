#include <bits/stdc++.h>
using namespace std;

int main(){
    string s="hadi";
    s.length();
    s.push_back('t');
    s.pop_back();
    string str;
    getline(cin, str); //input string with spaces, custom delimiter can be passed as third argument
}